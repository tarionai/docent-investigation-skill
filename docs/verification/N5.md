# Verification record — Node N5 (honesty anchor; pre-registered)

## Commands run
- Pre-registration committed BEFORE results (`c943b95`, `PRE_REGISTRATION.md`).
- `python -m pytest tests/test_anchor.py -q` → **6 passed** (incl. dependency-free Fisher exact).
- Anchor computed live in `scripts/run_investigation.py`; persisted to `data/results.json`.

## Result — 2×2 (N=100, judge blind to oracle)

|                  | resolved | not_resolved |
|------------------|----------|--------------|
| declared_success |    9 (A) |    10 (B)    |
| not_declared     |    5 (C) |    76 (D)    |

- `false_success_rate` = B/(A+B) = **0.526** (10 of 19 confident success claims were on tasks that failed)
- `declared_among_unresolved` = B/(B+D) = 0.116
- `resolved_rate_declared` = 0.474  vs  `resolved_rate_not_declared` = 0.062
- `fisher_p` = **5.7e-05** (highly significant association)
- **DECISION = INCONCLUSIVE** (per the pre-registered rule)

## Interpretation (honest, pre-registration-bound)
The pre-registered FALSE-SUCCESS FINDING required success-declarations to predict **lower** resolution
(an anti-informative pathology). The data shows the **opposite**: declaring success predicts an 8× higher
resolve rate (47% vs 6%, p<1e-4) — so the strict pathology finding is correctly NOT claimed → INCONCLUSIVE.
**Yet false successes are common in absolute terms: 53% of confident success declarations were wrong.**
The pre-registration prevented overclaiming in both directions. This is the eval-rigor result, not a null
dodge: a real, significant, nuanced measurement with the decision rule honored exactly.

## Limitation (pre-stated)
The oracle validates resolution, not the rubric's claim-detection fidelity; rigorous rubric-label
validation needs independent human labels (deferred). Single model / scaffold / benchmark.

## Next
N3 (skill prose, now able to cite real numbers) + N6 (report + publish + cold-clone reproduce).
