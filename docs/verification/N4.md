# Verification record — Node N4 (batch the bounded sample; flag-frequency)

## Commands run
- `python scripts/fetch_traces.py --out data --n 100` → 100 records + oracle. exit 0.
- `python scripts/run_investigation.py --data data --n 100` (LIVE) → ingested 100, evaluated 100, all
  100 verdicts returned. (Cosmetic print bug + a `get_agent_run(collection_id, ...)` arg fix were
  corrected; the eval itself completed and `data/results.json` was written.)
- Resume path `--collection-id 6b6b1e65-...` re-reads the existing collection without re-spending the eval.
- `python -m pytest tests -q` → **22 passed**.

## What was built
- `src/docent_investigation/batch.py` — `load_records`, `build_runs`, `join_rows` (mock-testable).
- `scripts/run_investigation.py` — live batch driver with a `--collection-id` resume path.
- `tests/test_batch.py` — 3 tests (build/join/load) with a mocked adapter.

## Result (public collection)
- Collection: https://docent.transluce.org/dashboard/6b6b1e65-e770-4049-ade9-9bd6ba538f4f
- N=100 OpenHands Verified runs (Nemotron-Nano). Oracle dist: resolved 14 / unresolved 52 / empty_patch 34.
- **Flag-frequency:** `declared_success` = **19/100 (19%)**; `not_declared` = 81/100.

## Honesty note
Judge ran with `include_metadata=False` (oracle blind). N reported exactly (100/100 verdicts). No silent
truncation. Cost borne server-side by the hosted Transluce account (gpt-5.4-mini judge).

## Next
N5 anchor computed from these rows (see N5.md).
