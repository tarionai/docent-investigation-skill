# Verification record — Node N0 (scaffold + local sanity harness)

## Commands run (verbatim)
- `python tools/plugin_sanity.py` → `PLUGIN SANITY: OK`, exit **0**.
- `python -m pytest tests/test_plugin_sanity.py -q` → `5 passed in 0.12s`, exit **0**.
  (pytest was not present in the active pyenv 3.11.9 interpreter; installed via `python -m pip install pytest`.)

## Files created
- `.claude-plugin/marketplace.json`
- `plugins/docent-investigation/.claude-plugin/plugin.json`
- `plugins/docent-investigation/.mcp.json`
- `plugins/docent-investigation/skills/investigation/{SKILL.md, rubric-reference.md, oracle-anchor.md}` (non-empty placeholders)
- `tools/plugin_sanity.py`
- `tests/test_plugin_sanity.py`
- `README.md`, `.gitignore`, `pyproject.toml`

## Invariants confirmed (N0 prompt checklist 1–6)
1. All three JSON manifests parse. ✔ (validator loads each; no JSON errors)
2. `plugin.json.name == "docent-investigation"`; `version` semver `0.1.0`. ✔
3. marketplace entry `version` == plugin `version` (`0.1.0`) and `source == "./plugins/docent-investigation"`. ✔
4. `.mcp.json.mcpServers.docent`: `type=="stdio"`, `command=="uv"`, `args` contains `"--from"`. ✔
5. Declared skill files (`SKILL.md`, `rubric-reference.md`, `oracle-anchor.md`) exist + non-empty. ✔
6. No `.mcp.local.json` / `docent.env` / `docent.env.*` present. ✔
Fail-closed proven: tests mutate version-match, empty a skill file, plant a forbidden file, and corrupt the
MCP command — each makes `check()` return errors.

## Deferred / not done in N0 (by design)
- Real skill prose (placeholders only) → node N3.
- Any SDK / network logic → nodes N1, N2.
- Live MCP-server start check (`uv tool run ... docent-mcp`) → spot-check at N1/N2 setup (ASSUMPTION in plan §9).

## Next executable node
**N1 — trace transform (`.traj + report.json → AgentRun`).** Status: unblocked for code; requires
downloading ONE real SWE-bench Verified trajectory fixture (read-only public data). Not blocked on Premise 2.
