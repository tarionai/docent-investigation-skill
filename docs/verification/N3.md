# Verification record — Node N3 (author the investigation skill prose)

## Commands run
- `python tools/plugin_sanity.py` → `PLUGIN SANITY: OK` (declared skill files still present + non-empty).
- `python -m pytest tests -q` → **22 passed**.

## What was built
- `plugins/docent-investigation/skills/investigation/SKILL.md` — full procedure (replaces the N0
  placeholder): pick behavior + oracle → ingest → author rubric (blind) → evaluate (include_metadata=False)
  → measure + anchor → report honestly. References the real `docent_investigation` modules + validated SDK calls.
- `rubric-reference.md` — exact create/evaluate/read SDK calls + the frozen reference rubric.
- `oracle-anchor.md` — pre-registration discipline, the 2×2, metrics, the worked-example numbers, blind spot.

## Invariants confirmed
- Skill references only validated SDK method names (create_rubric / start_rubric_eval_job /
  get_rubric_run_state / add_agent_runs / make_collection_public). ✔
- The "judge blind to oracle" rule is stated as the load-bearing honesty rule. ✔
- plugin_sanity still green (declared skill-file set intact). ✔
